function indicator = ddplot(X,Y)
%DDPLOT Summary of this function goes here
%   Detailed explanation goes here
Z = [X
    Y]

[szX1, ~] = size(X)
[szY1, ~] = size(Y)

indicator = zeros (szX1+szY1);
for i=1:szX1+szY1
    if i <= szX1
        indicator(i) = norm(Z(i) - X(j))
    else
        indicator()
end

